package com.clinic.raamish.utility

object Constant {
    // Use while converting ISO date string to Date object
    const val ISO_TO_DATE_FORMAT = "uuuu-MM-dd'T'HH:mm:ss.SSSX"
    // Use while converting Date object to date string
    const val DATE_TO_ISO_FORMAT = "yyyy-MM-dd'T'HH:mm:ss.SSS'Z'"
    const val DATE_TIME_FORMAT = "dd/MM/yyyy h:mm a"
}